import { Typography } from "@material-ui/core";

const Home = () => {
  return (
    <main className="home">
      <Typography variant="h2" />
      Quick About
    </main>
  );
};

export default Home;
